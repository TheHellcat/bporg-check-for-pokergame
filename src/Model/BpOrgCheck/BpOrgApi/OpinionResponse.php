<?php
declare(strict_types=1);

namespace App\Model\BpOrgCheck\BpOrgApi;

use Doctrine\Common\Collections\ArrayCollection;
use JMS\Serializer\Annotation as Serializer;
use Nelmio\ApiDocBundle\Annotation\Model;
use OpenApi\Annotations as OA;

/**
 * This was supposed to become the data model calls for deserializing the BP.org API response,
 * before I became too lazy and didn't do proper deserialization after all....
 */
class OpinionResponse
{}
